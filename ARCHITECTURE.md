# 🏗️ Архитектура проекта

## Обзор

Проект построен по модульной архитектуре с чёткими зонами ответственности.

```
┌─────────────────────────────────────────────────┐
│              360dialog API                      │
│         (WhatsApp Business API)                 │
└──────────────────┬──────────────────────────────┘
                   │ Webhook POST/GET
                   ▼
┌─────────────────────────────────────────────────┐
│              bot.py (Flask App)                 │
│  ┌───────────────────────────────────────────┐  │
│  │         webhook.py (Blueprint)            │  │
│  │  - GET /webhook  (верификация)            │  │
│  │  - POST /webhook (входящие сообщения)     │  │
│  │  - GET /health   (healthcheck)            │  │
│  └──────────────────┬────────────────────────┘  │
└─────────────────────┼───────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│          menu_handlers.py (Роутер)              │
│  ┌──────────────────────────────────────────┐   │
│  │  handle_incoming_message()               │   │
│  │    ├─ handle_text_message()              │   │
│  │    └─ handle_button_click()              │   │
│  └──────────────────────────────────────────┘   │
│                                                  │
│  Обработчики меню:                               │
│  • handle_main_menu()                            │
│  • handle_shift_menu()                           │
│  • handle_stats_menu()                           │
│  • handle_settings_menu()                        │
└──────────┬────────────┬──────────────────────────┘
           │            │
           ▼            ▼
┌──────────────┐  ┌──────────────────────────┐
│  utils/      │  │  config.py               │
│              │  │  (Переменные окружения)  │
│  ├─ state.py │  └──────────────────────────┘
│  │  (FSM)    │
│  │           │
│  ├─ api_360.py
│  │  (API клиент)
│  │  • send_text_message()
│  │  • send_buttons()
│  │  • send_list_message()
│  │
│  └─ sheets.py
│     (Google Sheets - TODO)
└──────────────┘
```

## Компоненты

### 1. bot.py - Точка входа
**Ответственность:** Запуск Flask приложения

```python
app = Flask(__name__)
app.register_blueprint(webhook_bp)
app.run(host=SERVER_HOST, port=SERVER_PORT)
```

**Функции:**
- Создание Flask приложения
- Регистрация Blueprint'ов
- Запуск веб-сервера
- Главная страница `/`

### 2. config.py - Конфигурация
**Ответственность:** Загрузка настроек из .env

```python
D360_API_KEY = os.getenv("D360_API_KEY")
D360_BASE_URL = os.getenv("D360_BASE_URL")
VERIFY_TOKEN = os.getenv("VERIFY_TOKEN")
```

**Функции:**
- Чтение переменных окружения
- Валидация обязательных параметров
- Парсинг списков (ADMIN_IDS)

### 3. webhook.py - Webhook обработчик
**Ответственность:** Приём запросов от 360dialog

**Endpoints:**

```python
GET  /webhook  # Верификация токена
POST /webhook  # Приём сообщений
GET  /health   # Healthcheck
```

**Поток данных:**
1. 360dialog отправляет POST запрос
2. Извлекается массив `messages`
3. Каждое сообщение передается в `handle_incoming_message()`

### 4. menu_handlers.py - Бизнес-логика
**Ответственность:** Обработка команд и меню

**Главный роутер:**
```python
handle_incoming_message(message)
  ├─ type: "text" → handle_text_message()
  └─ type: "interactive" → handle_button_click()
```

**Обработчики команд:**
```python
handle_text_message(user_id, text)
  ├─ "start" → handle_main_menu()
  ├─ "help" → handle_help()
  └─ FSM state → handle_name_input() / handle_hours_input()
```

**Обработчики кнопок:**
```python
handle_button_click(user_id, button_id)
  ├─ "main_menu" → handle_main_menu()
  ├─ "shift_menu" → handle_shift_menu()
  ├─ "stats_menu" → handle_stats_menu()
  └─ "settings_menu" → handle_settings_menu()
```

### 5. utils/state.py - FSM
**Ответственность:** Управление состоянием пользователей

**Структура состояния:**
```python
{
  "user_id": {
    "state": "waiting_name",  # Текущее состояние
    "data": {                  # Данные контекста
      "key": "value"
    }
  }
}
```

**Функции:**
```python
get_user_state(user_id)      # Получить состояние
set_user_state(user_id, state, data)  # Установить
clear_user_state(user_id)    # Очистить
update_user_data(user_id, key, value)  # Обновить данные
```

### 6. utils/api_360.py - API клиент
**Ответственность:** Отправка сообщений в WhatsApp

**Методы:**

```python
send_text_message(user_id, text)
# → POST {D360_BASE_URL}/v1/messages
# → {"to": user_id, "type": "text", "text": {"body": text}}

send_buttons(user_id, text, buttons)
# → POST {D360_BASE_URL}/v1/messages
# → {"type": "interactive", "interactive": {...}}

send_list_message(user_id, text, button_text, sections)
# → POST {D360_BASE_URL}/v1/messages
# → {"type": "interactive", "interactive": {"type": "list"}}
```

### 7. utils/sheets.py - Google Sheets
**Ответственность:** Экспорт в таблицы (TODO)

```python
export_to_sheet(data)       # Экспорт данных
read_from_sheet(range)      # Чтение данных
update_sheet_row(row, data) # Обновление строки
```

## Поток обработки сообщения

```
1. Пользователь отправляет сообщение в WhatsApp
   ↓
2. 360dialog получает сообщение
   ↓
3. 360dialog → POST /webhook (webhook.py)
   ↓
4. webhook.py → handle_incoming_message() (menu_handlers.py)
   ↓
5. menu_handlers.py определяет тип:
   - text → handle_text_message()
   - interactive → handle_button_click()
   ↓
6. Проверка состояния FSM (utils/state.py)
   ↓
7. Выполнение бизнес-логики
   ↓
8. Отправка ответа через API (utils/api_360.py)
   ↓
9. api_360.py → POST {D360_BASE_URL}/v1/messages
   ↓
10. 360dialog → WhatsApp → Пользователь получает ответ
```

## FSM (Finite State Machine)

Пример работы с состояниями:

```python
# Шаг 1: Пользователь нажимает "Изменить имя"
def handle_button_click(user_id, button_id):
    if button_id == "change_name":
        set_user_state(user_id, "waiting_name")
        send_text_message(user_id, "Введите новое имя:")

# Шаг 2: Пользователь отправляет текст
def handle_text_message(user_id, text):
    state = get_user_state(user_id)
    
    if state.get("state") == "waiting_name":
        # Сохраняем имя
        save_name_to_db(user_id, text)
        clear_user_state(user_id)
        send_text_message(user_id, f"✅ Имя изменено: {text}")
        handle_main_menu(user_id)
```

## Безопасность

1. **Верификация webhook:**
```python
if mode == 'subscribe' and token == VERIFY_TOKEN:
    return challenge, 200
```

2. **Проверка админов:**
```python
if user_id in ADMIN_IDS:
    # Админские функции
```

3. **Логирование:**
```python
logger.info(f"📨 Получено сообщение от {user_id}")
```

## Масштабирование

### Текущая архитектура (для старта):
- Состояния в памяти (dict)
- Один процесс Flask
- SQLite для данных (опционально)

### Для продакшена:
- Состояния в Redis
- Несколько worker'ов (Gunicorn)
- PostgreSQL для данных
- Message Queue (Celery + RabbitMQ)

## Добавление новой функции

### Пример: Добавить меню "Отчеты"

1. **Создать обработчик** в `menu_handlers.py`:
```python
def handle_reports_menu(user_id: str):
    text = "📊 *Отчеты*\n\nВыберите тип отчета:"
    buttons = [
        {"id": "report_daily", "title": "День"},
        {"id": "report_weekly", "title": "Неделя"},
        {"id": "main_menu", "title": "🔙 Назад"}
    ]
    send_buttons(user_id, text, buttons)
```

2. **Добавить роутинг** в `handle_button_click()`:
```python
elif button_id == 'reports_menu':
    handle_reports_menu(user_id)
```

3. **Добавить кнопку** в главное меню:
```python
def handle_main_menu(user_id: str):
    buttons = [
        {"id": "shift_menu", "title": "🚜 Смена"},
        {"id": "stats_menu", "title": "📊 Статистика"},
        {"id": "reports_menu", "title": "📄 Отчеты"},  # Новая кнопка
    ]
```

## Debugging

### Просмотр состояний:
```python
from utils.state import get_all_states
print(get_all_states())
```

### Тест отправки:
```python
from utils.api_360 import send_text_message
send_text_message("79991234567", "Тест")
```

### Логи:
```bash
tail -f bot.log
```

## Зависимости

```
Flask → Веб-сервер
requests → HTTP клиент для 360dialog
python-dotenv → Загрузка .env
gspread → Google Sheets (будущее)
```

---

**Архитектура готова к расширению и масштабированию!** 🚀

