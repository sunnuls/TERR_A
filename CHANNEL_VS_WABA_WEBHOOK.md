# 📋 ЧТО ДОЛЖНО БЫТЬ В Channel Webhook И Waba Webhook

## 🔍 ДВА РАЗНЫХ ПОЛЯ:

### 1️⃣ Channel Webhook (для конкретного канала/номера):

**Где находится:**
- В разделе "Details" конкретного WhatsApp номера
- Показывает "WABA Channel External ID: 876646062208778"

**Что должно быть написано:**

**Webhook URL:**
```
https://untransformable-tomeka-homogonously.ngrok-free.dev/webhook
```

**Headers:**
- Key: `X-Verify-Token`
- Value: `terra_verify123`

**Назначение:**
- Получает сообщения для конкретного WhatsApp номера
- Это то, что нужно настроить для работы бота

---

### 2️⃣ Waba Webhook (для всего аккаунта):

**Где находится:**
- В разделе "Details" WhatsApp Business Account
- Показывает "WhatsApp Business Account ID: 809800252026037"

**Что должно быть написано:**

**Webhook URL:**
```
https://untransformable-tomeka-homogonously.ngrok-free.dev/webhook
```
(ТОТ ЖЕ URL, что и в Channel Webhook)

**Headers:**
- Key: `X-Verify-Token`
- Value: `terra_verify123`

**Назначение:**
- Получает сообщения для всего аккаунта (всех номеров)
- Можно настроить, но обычно достаточно Channel Webhook

---

## ✅ ЧТО НУЖНО СДЕЛАТЬ:

### Приоритет: Channel Webhook

**Настройте Channel Webhook:**

1. **Webhook URL:**
   ```
   https://untransformable-tomeka-homogonously.ngrok-free.dev/webhook
   ```

2. **Headers:**
   - Key: `X-Verify-Token`
   - Value: `terra_verify123`
   - Нажмите "+" чтобы добавить

3. **Сохраните:** Нажмите "Save"

### Опционально: Waba Webhook

Если хотите, можете настроить и Waba Webhook с теми же значениями, но это не обязательно, если Channel Webhook уже настроен.

---

## 🎯 ИТОГО:

**Channel Webhook (ОБЯЗАТЕЛЬНО):**
- URL: `https://untransformable-tomeka-homogonously.ngrok-free.dev/webhook`
- Headers: `X-Verify-Token` = `terra_verify123`

**Waba Webhook (опционально):**
- Можно настроить так же, но не обязательно
- Используйте тот же URL и те же Headers

---

## ⚠️ ВАЖНО:

- **Channel Webhook** - это то, что нужно настроить для работы бота
- **Waba Webhook** - можно настроить, но не обязательно
- Оба используют ОДИН И ТОТ ЖЕ URL и Headers
- Главное - настроить **Channel Webhook**

