# Настройка Google Sheets интеграции

## 📋 Обзор

Бот использует Google Sheets API для сохранения данных о рабочих часах. Данные автоматически записываются в таблицу Google Sheets после подтверждения пользователем.

**Структура записи:**
```
Дата и время | Телефон | Работа | Смена | Часов
2024-11-07 12:34:56 | 79991234567 | Поле | 8-16 | 8
```

---

## 🚀 Быстрая настройка

### 1. Создание Service Account в Google Cloud

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите **Google Sheets API**:
   - Перейдите: `APIs & Services` → `Enable APIs and Services`
   - Найдите "Google Sheets API"
   - Нажмите "Enable"

4. Создайте Service Account:
   - Перейдите: `IAM & Admin` → `Service Accounts`
   - Нажмите "Create Service Account"
   - Имя: `whatsapp-bot-sheets`
   - Описание: `Service account для WhatsApp бота`
   - Нажмите "Create and Continue"

5. Создайте JSON ключ:
   - В списке Service Accounts найдите созданный аккаунт
   - Нажмите на email аккаунта
   - Перейдите на вкладку "Keys"
   - Нажмите "Add Key" → "Create new key"
   - Выберите тип: **JSON**
   - Нажмите "Create"
   - Файл `your-project-xxxxx.json` будет скачан

### 2. Создание Google Таблицы

1. Откройте [Google Sheets](https://sheets.google.com/)
2. Создайте новую таблицу: "Учёт рабочих часов" (или любое имя)
3. Скопируйте **ID таблицы** из URL:
   ```
   https://docs.google.com/spreadsheets/d/1AbCdEfGhIjKlMnOpQrStUvWxYz/edit
                                      ^^^^^^^^^^^^^^^^^^^^^^^^^
                                      Это ваш SHEET_ID
   ```

4. **Важно!** Предоставьте доступ Service Account:
   - Нажмите "Share" (Поделиться) в правом верхнем углу
   - Вставьте email из JSON ключа (вида: `whatsapp-bot-sheets@your-project.iam.gserviceaccount.com`)
   - Выберите роль: **Editor** (Редактор)
   - Снимите галочку "Notify people" (Уведомлять)
   - Нажмите "Share"

### 3. Настройка .env файла

#### Вариант A: Путь к JSON файлу (рекомендуется для локальной разработки)

```env
SHEETS_CREDENTIALS=credentials.json
SHEET_ID=1AbCdEfGhIjKlMnOpQrStUvWxYz
SHEETS_ENABLED=true
```

Поместите скачанный JSON файл в корень проекта и переименуйте в `credentials.json`.

#### Вариант B: Base64 encoded JSON (рекомендуется для продакшена/Docker)

Закодируйте JSON в base64:

**Linux/Mac:**
```bash
cat your-project-xxxxx.json | base64 -w 0
```

**Windows (PowerShell):**
```powershell
[Convert]::ToBase64String([System.IO.File]::ReadAllBytes("your-project-xxxxx.json"))
```

Добавьте в `.env`:
```env
SHEETS_CREDENTIALS=eyJ0eXBlIjogInNlcnZpY2VfYWNjb3VudCIsICJwcm9qZWN0X2lkIjogInlvdXItcHJvamVjdCIsIC4uLn0=
SHEET_ID=1AbCdEfGhIjKlMnOpQrStUvWxYz
SHEETS_ENABLED=true
```

#### Вариант C: Прямой JSON (не рекомендуется)

```env
SHEETS_CREDENTIALS={"type":"service_account","project_id":"your-project",...}
SHEET_ID=1AbCdEfGhIjKlMnOpQrStUvWxYz
SHEETS_ENABLED=true
```

### 4. Установка зависимостей

```bash
pip install -r requirements_whatsapp.txt
```

### 5. Запуск бота

```bash
python bot.py
```

В логах вы должны увидеть:

```
[SHEETS] 🔄 Инициализация подключения к Google Sheets...
[SHEETS] ✓ Авторизация выполнена успешно
[SHEETS] ✓ Подключение к Google Sheets установлено
[SHEETS] ✓ Таблица открыта: 'Учёт рабочих часов'
[SHEETS] ✓ Лист 'Данные' найден
[SHEETS] ✅ Инициализация Google Sheets завершена успешно
✅ Google Sheets готов к работе
```

---

## 📊 Структура таблицы

Бот автоматически создаёт лист "Данные" со следующей структурой:

| A | B | C | D | E |
|---|---|---|---|---|
| **Дата и время** | **Телефон** | **Работа** | **Смена** | **Часов** |
| 2024-11-07 12:34:56 | 79991234567 | Поле | 8-16 | 8 |
| 2024-11-07 14:20:10 | 79992223333 | Кабачок | 16-00 | 6 |
| 2024-11-07 16:45:30 | 79991234567 | Картошка | 8-16 | 12 |

### Автоматические действия:

- ✅ Создание листа "Данные", если его нет
- ✅ Добавление заголовков в первую строку
- ✅ Автоматическая запись новых данных

---

## 🔧 Настройки

### Включение/отключение интеграции

```env
# Включить (по умолчанию)
SHEETS_ENABLED=true

# Отключить (бот будет работать без сохранения в Google Sheets)
SHEETS_ENABLED=false
```

При отключении Google Sheets:
- Бот продолжит работу нормально
- Данные будут логироваться в `bot.log`
- Данные будут сохраняться в `sheets_fallback.log`

### Fallback при ошибках

Если запись в Google Sheets не удаётся, данные автоматически сохраняются в:
- **Лог файл:** `bot.log`
- **Fallback файл:** `sheets_fallback.log` (формат CSV)

Пример `sheets_fallback.log`:
```csv
2024-11-07 12:34:56,79991234567,Поле,8-16,8
2024-11-07 14:20:10,79992223333,Кабачок,16-00,6
```

---

## 🧪 Тестирование

### Проверка подключения

Запустите бота и проверьте логи:

```bash
python bot.py
```

Ожидаемый вывод:
```
[SHEETS] 🔄 Инициализация подключения к Google Sheets...
[SHEETS] ✓ Авторизация выполнена успешно
[SHEETS] ✓ Подключение к Google Sheets установлено
[SHEETS] ✓ Таблица открыта: 'Ваша таблица'
[SHEETS] ✓ Лист 'Данные' найден (или создан)
[SHEETS] ✅ Инициализация Google Sheets завершена успешно
```

### Тестирование записи

1. Запустите бота
2. Отправьте "start" в WhatsApp
3. Пройдите весь путь FSM:
   - Выберите "Работа"
   - Выберите тип работы (например, "Поле")
   - Выберите смену (например, "Смена 1 (8-16)")
   - Выберите часы (например, "8 часов")
   - Подтвердите

4. Проверьте логи:
```
[SHEETS] 📝 Сохранение записи в Google Sheets:
[SHEETS]   ▸ Время: 2024-11-07 12:34:56
[SHEETS]   ▸ Телефон: 79991234567
[SHEETS]   ▸ Работа: Поле
[SHEETS]   ▸ Смена: 8-16
[SHEETS]   ▸ Часов: 8
[SHEETS] ✅ Запись успешно сохранена в Google Sheets
```

5. Откройте Google Таблицу - новая строка должна появиться

---

## ❓ Troubleshooting

### Ошибка: "Таблица не найдена"

```
[SHEETS] ❌ Таблица с ID '...' не найдена
```

**Решение:**
1. Проверьте правильность `SHEET_ID` в `.env`
2. Убедитесь, что Service Account имеет доступ к таблице
3. Проверьте email Service Account в JSON ключе
4. Откройте Google Таблицу → Share → добавьте email Service Account как Editor

### Ошибка: "Ошибка декодирования base64"

```
[SHEETS] ❌ Ошибка декодирования base64
```

**Решение:**
1. Проверьте, что base64 строка скопирована полностью
2. Убедитесь, что нет переносов строк в base64
3. Попробуйте вариант с файлом вместо base64

### Ошибка: "API ошибка Google Sheets"

```
[SHEETS] ❌ API ошибка Google Sheets
```

**Решение:**
1. Убедитесь, что Google Sheets API включен в проекте
2. Проверьте квоты API в Google Cloud Console
3. Проверьте права доступа Service Account

### Предупреждение: "Google Sheets не инициализированы"

```
[SHEETS] ⚠️ Google Sheets не инициализированы, данные будут только залогированы
```

**Решение:**
1. Проверьте `SHEETS_ENABLED=true` в `.env`
2. Проверьте наличие `SHEETS_CREDENTIALS` и `SHEET_ID`
3. Проверьте логи выше на предмет ошибок инициализации

### Данные не появляются в таблице

**Проверьте:**
1. Логи бота - есть ли сообщение `[SHEETS] ✅ Запись успешно сохранена`
2. Правильный лист открыт (должен быть "Данные")
3. Обновите страницу Google Sheets (Ctrl+R)
4. Проверьте `sheets_fallback.log` - если данные там, значит запись в Sheets не работает

---

## 🔐 Безопасность

### Важно!

1. **НЕ коммитьте** JSON ключи в Git:
   ```bash
   echo "credentials.json" >> .gitignore
   echo ".env" >> .gitignore
   ```

2. **Ограничьте права** Service Account:
   - Предоставляйте доступ только к необходимым таблицам
   - Используйте роль Editor, а не Owner

3. **Храните ключи безопасно:**
   - В продакшене используйте переменные окружения
   - В Docker используйте secrets
   - В CI/CD используйте защищённые переменные

4. **Регулярно ротируйте ключи:**
   - Создавайте новые ключи периодически
   - Удаляйте старые ключи из Google Cloud

---

## 📈 Расширенные возможности

### Получение статистики

```python
from utils.sheets import get_stats

stats = get_stats()
print(stats)
# {
#     "status": "ok",
#     "total_records": 42,
#     "sheet_title": "Данные",
#     "spreadsheet_title": "Учёт рабочих часов",
#     "url": "https://docs.google.com/spreadsheets/d/..."
# }
```

### Получение URL таблицы

```python
from utils.sheets import get_sheet_url

url = get_sheet_url()
print(f"Таблица: {url}")
```

### Проверка инициализации

```python
from utils.sheets import is_initialized

if is_initialized():
    print("Google Sheets готов")
else:
    print("Google Sheets не инициализирован")
```

---

## 📝 Примеры использования

### Пример 1: Ручная запись

```python
from utils.sheets import save_entry

success = save_entry(
    phone="79991234567",
    work="Поле",
    shift="8-16",
    hours="8"
)

if success:
    print("Запись сохранена!")
else:
    print("Ошибка сохранения")
```

### Пример 2: Интеграция с FSM

```python
# menu_handlers.py
def handle_confirm_save(phone: str, confirmed: bool):
    if confirmed:
        work = get_user_data(phone, 'work')
        shift = get_user_data(phone, 'shift')
        hours = get_user_data(phone, 'hours')
        
        # Сохранение в Google Sheets
        success = save_entry(phone, work, shift, hours)
        
        if success:
            send_text_message(phone, "✅ Данные сохранены в Google Sheets!")
        else:
            send_text_message(phone, "⚠️ Данные сохранены локально")
```

---

## 🎯 Best Practices

1. **Мониторинг**
   - Отслеживайте логи `[SHEETS]`
   - Проверяйте `sheets_fallback.log` на наличие данных
   - Настройте алерты при ошибках API

2. **Резервное копирование**
   - Регулярно экспортируйте таблицу
   - Настройте автоматическое копирование в Google Drive
   - Сохраняйте копии локально

3. **Производительность**
   - Не перегружайте API (лимит ~60 запросов/минуту/пользователя)
   - Используйте батчинг для массовых операций
   - Кэшируйте данные при необходимости

4. **Валидация**
   - Проверяйте данные перед записью
   - Обрабатывайте все исключения
   - Логируйте все операции

---

## ✅ Checklist готовности

- [ ] Google Cloud проект создан
- [ ] Google Sheets API включен
- [ ] Service Account создан
- [ ] JSON ключ скачан
- [ ] Google Таблица создана
- [ ] Service Account добавлен в Share с ролью Editor
- [ ] `SHEET_ID` скопирован из URL
- [ ] `.env` файл настроен
- [ ] Зависимости установлены (`pip install -r requirements_whatsapp.txt`)
- [ ] Бот запущен без ошибок
- [ ] Тестовая запись создана успешно
- [ ] Данные появились в Google Таблице

---

## 📚 Дополнительные ресурсы

- [Google Sheets API Documentation](https://developers.google.com/sheets/api)
- [gspread Documentation](https://docs.gspread.org/)
- [Service Accounts](https://cloud.google.com/iam/docs/service-accounts)
- [Google Cloud Console](https://console.cloud.google.com/)

---

**Готово!** Google Sheets интеграция настроена и готова к работе. 🎉

