# 📍 Куда вставить Verify Token в 360dialog

## 🔧 Инструкция по настройке:

### В разделе "Channel Webhook":

1. **Webhook URL:**
   ```
   https://untransformable-tomeka-homogonously.ngrok-free.dev/webhook
   ```

2. **Headers (секция ниже Webhook URL):**
   
   В поле **"Key"** введите:
   ```
   X-Verify-Token
   ```
   
   В поле **"Value"** введите:
   ```
   terra_verify123
   ```
   
   Нажмите кнопку **"+"** чтобы добавить заголовок

3. **Сохраните настройки:**
   Нажмите кнопку **"Save"** внизу секции

## 📋 Альтернативные варианты Key:

Если `X-Verify-Token` не работает, попробуйте:

- `Verify-Token`
- `X-Hub-Signature` 
- `Authorization`
- `X-Webhook-Token`

## ✅ Проверка:

После сохранения:
1. Убедитесь, что бот запущен: `python bot_polya_whatsapp.py`
2. Отправьте боту `start` в WhatsApp
3. Бот должен ответить

## 🔍 Если не работает:

Если 360dialog не поддерживает заголовки, Verify Token проверяется автоматически при GET запросе на `/webhook` с параметром `hub.verify_token`. В этом случае просто сохраните Webhook URL без заголовков.

