# Настройка бота для 360dialog

## ✅ Ваши данные

**API Key (токен):** `DQSi7mQYdGwmx4rEqvnQRRJrAK`  
**Webhook URL:** `https://waba-v2.360dialog.io`

## 📋 Что нужно сделать

### Шаг 1: Получить Phone Number ID

1. Зайдите в панель 360dialog: https://hub.360dialog.com/
2. Войдите в свой аккаунт
3. Выберите ваш WhatsApp номер
4. Найдите и скопируйте **Phone Number ID** (это числовой ID)

### Шаг 2: Создать файл .env

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# WhatsApp Business API настройки (360dialog)
WHATSAPP_TOKEN=DQSi7mQYdGwmx4rEqvnQRRJrAK
WHATSAPP_PHONE_ID=ВАSH_PHONE_NUMBER_ID_ИЗ_360DIALOG
VERIFY_TOKEN=terra_bot_verify_token_2024

# Сервер
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# Админы - ЗАМЕНИТЕ НА ВАШИ НОМЕРА!
# Формат: 79991234567 (без +, пробелов и дефисов)
ADMIN_IDS=79991234567

# Таймзона
TZ=Europe/Moscow

# Google Sheets (опционально)
OAUTH_CLIENT_JSON=oauth_client.json
TOKEN_JSON_PATH=token.json
DRIVE_FOLDER_ID=
EXPORT_PREFIX=WorkLog

# Автоматический экспорт
AUTO_EXPORT_ENABLED=false
AUTO_EXPORT_CRON=0 9 * * 1
```

### Шаг 3: Настроить Webhook в 360dialog

1. Зайдите в панель 360dialog
2. Перейдите в раздел "Webhooks" или "Configuration"
3. Настройте webhook:
   - **URL**: Ваш публичный URL (например: `https://ваш-домен.com/` или используйте ngrok для тестов)
   - **Verify Token**: `terra_bot_verify_token_2024` (тот же, что в .env)

### Шаг 4: Установить зависимости

```bash
pip install -r requirements_whatsapp.txt
```

### Шаг 5: Запустить бота

```bash
python bot_polya_whatsapp.py
```

## 🔧 Для локального тестирования (ngrok)

Если запускаете на локальном компьютере, используйте ngrok:

```bash
# В одном терминале запустите бота
python bot_polya_whatsapp.py

# В другом терминале запустите ngrok
ngrok http 8000
```

Скопируйте HTTPS URL из ngrok (например: `https://abc123.ngrok.io`) и укажите его в настройках webhook 360dialog.

## ⚠️ Важно

1. **Phone Number ID обязателен** - без него бот не запустится
2. **ADMIN_IDS** - замените на реальные номера администраторов в формате `79991234567`
3. **Webhook URL** должен быть публично доступен по HTTPS
4. **Verify Token** должен совпадать в .env файле и в настройках 360dialog

## 🆘 Если что-то не работает

### Бот не запускается
- Проверьте, что файл `.env` создан и заполнен
- Убедитесь, что указан правильный `WHATSAPP_PHONE_ID`

### Бот не отвечает на сообщения
- Проверьте webhook в панели 360dialog
- Убедитесь, что ваш сервер доступен по HTTPS
- Проверьте логи бота на наличие ошибок

### Как проверить, что всё работает
- Отправьте боту сообщение: `start`
- Бот должен ответить приветствием и попросить ввести имя

## 📱 Номера телефонов

Все номера указываются в формате **без символов**, только цифры:
- ✅ Правильно: `79991234567`
- ❌ Неправильно: `+7 999 123-45-67`

