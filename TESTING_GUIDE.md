# 🧪 Руководство по тестированию

## Быстрая проверка работоспособности

### 1. Проверка конфигурации

```bash
python -c "import config; print('✅ Config OK')"
```

Должно вывести:
```
✅ Конфигурация загружена успешно
📡 360dialog API URL: https://waba-v2.360dialog.io
🔑 API Key: DQSi7mQYdG...
👥 Админов: 1
✅ Config OK
```

### 2. Проверка импортов

```bash
python -c "
from webhook import webhook_bp
from menu_handlers import handle_main_menu
from utils.state import get_user_state
from utils.api_360 import send_text_message
print('✅ All imports OK')
"
```

### 3. Проверка Flask сервера

```bash
python bot.py
```

Откройте в браузере: http://localhost:8000

Должна открыться страница с зелёным статусом "✅ Сервер работает"

### 4. Healthcheck endpoint

В другом терминале:
```bash
curl http://localhost:8000/health
```

Ответ:
```json
{
  "status": "healthy",
  "service": "WhatsApp Bot 360dialog"
}
```

### 5. Webhook верификация (тест)

```bash
curl "http://localhost:8000/webhook?hub.mode=subscribe&hub.verify_token=terra_bot_verify_token_2024&hub.challenge=test123"
```

Должно вернуть: `test123`

## Тестирование API клиента

### Создайте файл `test_api.py`:

```python
# test_api.py
import sys
from utils.api_360 import send_text_message, send_buttons

def test_send_text():
    """Тест отправки текста"""
    print("🧪 Тест: отправка текста...")
    
    # ЗАМЕНИТЕ на свой номер!
    test_user = "79991234567"
    
    result = send_text_message(test_user, "🧪 Тест отправки текста")
    
    if result:
        print("✅ Текст отправлен")
    else:
        print("❌ Ошибка отправки")
    
    return result

def test_send_buttons():
    """Тест отправки кнопок"""
    print("🧪 Тест: отправка кнопок...")
    
    test_user = "79991234567"  # ЗАМЕНИТЕ!
    
    buttons = [
        {"id": "btn1", "title": "Кнопка 1"},
        {"id": "btn2", "title": "Кнопка 2"}
    ]
    
    result = send_buttons(test_user, "🧪 Тест кнопок", buttons)
    
    if result:
        print("✅ Кнопки отправлены")
    else:
        print("❌ Ошибка отправки")
    
    return result

if __name__ == "__main__":
    print("=" * 50)
    print("🚀 Тестирование API 360dialog")
    print("=" * 50)
    
    # Запускаем тесты
    test1 = test_send_text()
    test2 = test_send_buttons()
    
    print("\n" + "=" * 50)
    if test1 and test2:
        print("✅ Все тесты пройдены!")
    else:
        print("❌ Некоторые тесты не прошли")
    print("=" * 50)
```

### Запустите:

```bash
python test_api.py
```

**Важно:** Замените `79991234567` на свой реальный номер!

## Тестирование FSM (состояния)

### Создайте файл `test_fsm.py`:

```python
# test_fsm.py
from utils.state import (
    get_user_state,
    set_user_state,
    clear_user_state,
    update_user_data,
    get_user_data
)

def test_fsm():
    """Тест машины состояний"""
    test_user = "test_user_123"
    
    print("🧪 Тест FSM...")
    
    # Тест 1: Начальное состояние
    state = get_user_state(test_user)
    assert state["state"] is None
    print("✅ Начальное состояние OK")
    
    # Тест 2: Установка состояния
    set_user_state(test_user, "waiting_name", {"step": 1})
    state = get_user_state(test_user)
    assert state["state"] == "waiting_name"
    assert state["data"]["step"] == 1
    print("✅ Установка состояния OK")
    
    # Тест 3: Обновление данных
    update_user_data(test_user, "name", "Иван")
    name = get_user_data(test_user, "name")
    assert name == "Иван"
    print("✅ Обновление данных OK")
    
    # Тест 4: Очистка состояния
    clear_user_state(test_user)
    state = get_user_state(test_user)
    assert state["state"] is None
    assert state["data"] == {}
    print("✅ Очистка состояния OK")
    
    print("\n✅ Все FSM тесты пройдены!")

if __name__ == "__main__":
    test_fsm()
```

### Запустите:

```bash
python test_fsm.py
```

## Полное интеграционное тестирование

### 1. Запустите бота

```bash
python bot.py
```

Оставьте терминал открытым.

### 2. Настройте ngrok (для локального тестирования)

В новом терминале:
```bash
ngrok http 8000
```

Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

### 3. Настройте webhook в 360dialog

1. Зайдите на https://hub.360dialog.com/
2. Войдите в аккаунт
3. Выберите свой WhatsApp номер
4. Перейдите в раздел "Webhooks"
5. Укажите:
   - **URL**: `https://abc123.ngrok.io/webhook`
   - **Verify Token**: `terra_bot_verify_token_2024`
6. Нажмите "Verify"

Должен появиться зелёный статус "Connected" ✅

### 4. Отправьте тестовое сообщение

1. Найдите номер вашего бота в панели 360dialog
2. Откройте WhatsApp на телефоне
3. Отправьте боту: `start`

**Ожидаемый результат:**
Бот должен ответить главным меню с кнопками:
```
👋 Добро пожаловать в TERRA Bot!

Выберите действие:

[🚜 Смена] [📊 Статистика] [⚙️ Настройки]
```

### 5. Проверьте логи

В терминале с ботом вы должны увидеть:
```
📥 Получен запрос верификации webhook: mode=subscribe, token=***
✅ Webhook верифицирован успешно
📨 Получен webhook: {...}
👤 Сообщение от 79991234567, тип: text
💬 Текст от 79991234567: start
📋 Главное меню для 79991234567
📤 Отправка кнопок пользователю 79991234567
✅ Кнопки отправлены 79991234567
```

### 6. Тест кнопок

Нажмите на кнопку "🚜 Смена"

**Ожидаемый результат:**
```
🚜 Учёт рабочей смены

Выберите действие:

[▶️ Начать смену] [⏹ Завершить смену] [🔙 Назад]
```

## Проверка обработки ошибок

### 1. Неверный токен webhook

```bash
curl "http://localhost:8000/webhook?hub.mode=subscribe&hub.verify_token=WRONG_TOKEN&hub.challenge=test"
```

Должно вернуть: `Forbidden` (403)

### 2. Пустой POST запрос

```bash
curl -X POST http://localhost:8000/webhook \
  -H "Content-Type: application/json" \
  -d '{}'
```

Должно вернуть: `{"status":"ok"}` (200)

### 3. Неизвестная команда

Отправьте боту: `xyz123`

Бот должен ответить главным меню (fallback behavior).

## Стресс-тестирование

### Множественные запросы

```bash
for i in {1..10}; do
  curl http://localhost:8000/health &
done
wait
```

Все запросы должны вернуть `200 OK`.

## Чеклист готовности к продакшену

- [ ] Все unit-тесты прошли
- [ ] Webhook верифицирован в 360dialog
- [ ] Бот отвечает на сообщения
- [ ] Кнопки работают
- [ ] Логи пишутся корректно
- [ ] .env файл настроен
- [ ] ADMIN_IDS указаны верно
- [ ] База данных (если используется) подключена
- [ ] Google Sheets (если используется) настроены
- [ ] HTTPS настроен (для продакшена)
- [ ] Мониторинг настроен

## Отладка проблем

### Бот не запускается:

```bash
# Проверьте .env
cat .env

# Проверьте зависимости
pip list | grep Flask
pip list | grep requests

# Запустите с verbose
python -u bot.py
```

### Бот не отвечает:

```bash
# Проверьте логи
tail -n 50 bot.log

# Проверьте webhook статус в 360dialog
# Должен быть "Connected"

# Проверьте ngrok
curl https://your-ngrok-url.ngrok.io/health
```

### Ошибки API:

```python
# Проверьте API ключ
python -c "from config import D360_API_KEY; print(D360_API_KEY)"

# Тест прямого запроса
import requests
headers = {"D360-API-KEY": "ваш_ключ"}
r = requests.get("https://waba-v2.360dialog.io/v1/health", headers=headers)
print(r.status_code, r.text)
```

## Мониторинг в продакшене

### Проверка доступности:

```bash
# Каждые 60 секунд
watch -n 60 'curl -s http://your-domain.com/health | jq'
```

### Анализ логов:

```bash
# Ошибки
grep "ERROR" bot.log

# Сообщения от пользователей
grep "Сообщение от" bot.log

# API вызовы
grep "Отправка" bot.log
```

---

## ✅ Результат успешного тестирования

Если всё работает правильно, вы должны увидеть:

1. ✅ Сервер запущен на порту 8000
2. ✅ Healthcheck возвращает `{"status": "healthy"}`
3. ✅ Webhook верифицирован в 360dialog
4. ✅ Бот отвечает на "start"
5. ✅ Кнопки меню работают
6. ✅ Логи пишутся в bot.log
7. ✅ Состояния пользователей сохраняются

**Поздравляю! Бот готов к использованию! 🎉**

