# 📍 Что должно быть в Channel Webhook

## 🔧 Настройка Channel Webhook:

### 1. Channel Webhook URL:

В поле **"Channel Webhook"** должно быть:

```
https://untransformable-tomeka-homogonously.ngrok-free.dev/webhook
```

**Важно:**
- ✅ URL должен заканчиваться на `/webhook`
- ✅ Это адрес, куда 360dialog будет отправлять входящие сообщения
- ✅ Убедитесь, что ngrok запущен и показывает этот URL

### 2. Headers (секция ниже URL):

В секции **"Headers"** добавьте:

**Key:**
```
X-Verify-Token
```

**Value:**
```
terra_verify123
```

**Как добавить:**
1. Введите `X-Verify-Token` в поле "Key"
2. Введите `terra_verify123` в поле "Value"
3. Нажмите кнопку **"+"** справа от поля Value
4. Заголовок появится в списке выше

### 3. Сохранение:

Нажмите кнопку **"Save"** внизу страницы

## ✅ Итоговая настройка должна выглядеть так:

```
Channel Webhook:
├── URL: https://untransformable-tomeka-homogonously.ngrok-free.dev/webhook
└── Headers:
    ├── Key:   X-Verify-Token
    └── Value: terra_verify123
```

## 🔍 Проверка:

После сохранения:

1. **Убедитесь, что бот запущен:**
   ```powershell
   python bot_polya_whatsapp.py
   ```

2. **Убедитесь, что ngrok запущен:**
   - Должен показывать: `https://untransformable-tomeka-homogonously.ngrok-free.dev`

3. **Отправьте боту `start` в WhatsApp**

4. **Проверьте логи бота:**
   - Должны появиться записи о входящем сообщении

5. **Проверьте веб-интерфейс ngrok:**
   - Откройте: http://127.0.0.1:4040
   - Вы увидите все запросы к вашему боту

## ⚠️ Если не работает:

- Проверьте, что URL правильный (скопируйте из ngrok)
- Проверьте, что ngrok запущен
- Проверьте, что бот запущен на порту 8000
- Проверьте логи бота на наличие ошибок

