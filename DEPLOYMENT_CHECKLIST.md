# ✅ Чек-лист запуска бота

## Изменённые/Созданные файлы

### Новые файлы:
- ✅ `bot.py` — Главный Flask-сервер с вебхуками
- ✅ `constants.py` — Централизованные константы
- ✅ `menu_handlers.py` — Обработчики меню
- ✅ `requirements.txt` — Зависимости Python
- ✅ `run.bat` — Автозапуск бота + ngrok
- ✅ `start_ngrok.bat` — Запуск ngrok туннеля
- ✅ `.gitignore` — Игнорирование временных файлов
- ✅ `README.md` — Полная документация
- ✅ `QUICKSTART_RUS.md` — Быстрый старт на русском
- ✅ `DEPLOYMENT_CHECKLIST.md` — Этот чек-лист

### Новые директории и модули:
- ✅ `data/` — Хранилище JSON данных
- ✅ `data/.gitkeep` — Маркер для Git
- ✅ `storage/` — Модуль учета смен
- ✅ `storage/__init__.py`
- ✅ `storage/attendance.py` — Работа с attendance.json
- ✅ `scripts/` — Тестовые скрипты
- ✅ `scripts/__init__.py`
- ✅ `scripts/mock_payloads.py` — Эмуляция вебхуков

### Обновлённые файлы:
- ✅ `utils/api_360.py` — HTTP-клиент 360dialog

---

## 📋 Команды для запуска (Windows PowerShell)

### 1️⃣ Создание виртуального окружения
```powershell
python -m venv .venv
```

### 2️⃣ Активация виртуального окружения
```powershell
.\.venv\Scripts\Activate.ps1
```

**Если ошибка ExecutionPolicy:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 3️⃣ Установка зависимостей
```powershell
pip install -r requirements.txt
```

### 4️⃣ Копирование и заполнение .env
```powershell
copy env.example .env
notepad .env
```

**Заполните в .env:**
- `D360_API_KEY` — ваш API ключ от 360dialog
- `VERIFY_TOKEN` — придуманный вами секретный токен (например: `my_secret_token_abc123`)

### 5️⃣ Запуск бота
```powershell
python bot.py
```

### 6️⃣ Запуск ngrok (в отдельном окне)
```powershell
.\start_ngrok.bat
```
Или напрямую:
```powershell
ngrok http 8000
```

### 7️⃣ Настройка вебхука в 360dialog

1. Откройте http://localhost:4040 (админка ngrok)
2. Скопируйте **HTTPS URL** (например: `https://abc-123-def.ngrok.io`)
3. Откройте [hub.360dialog.com](https://hub.360dialog.com)
4. Перейдите в **Webhooks** или **Configuration**
5. Заполните:
   - **Webhook URL:** `https://ваш-ngrok-url.ngrok.io/webhook`
   - **Verify Token:** значение из `.env` (например: `my_secret_token_abc123`)
6. Включите события:
   - ✅ **Incoming messages**
   - ✅ **Message status**
7. Нажмите **Save** или **Verify and Save**

### 8️⃣ Тестирование

**Через WhatsApp:**
1. Откройте WhatsApp на телефоне
2. Напишите на номер, подключенный к 360dialog
3. Отправьте текст: **"меню"**
4. Должны прийти интерактивные кнопки
5. Нажмите **"Заполнить за сегодня"**
6. Выберите смену из списка
7. Получите подтверждение **"Смена записана ✅"**

**Через mock_payloads.py (локально):**
```powershell
python scripts/mock_payloads.py full
```

---

## 🔍 Проверка работоспособности

### Health Check
```powershell
curl http://localhost:8000/health
```

**Ожидаемый ответ:**
```json
{"status": "ok", "service": "whatsapp-bot", "mode": "dev"}
```

### Проверка корневого endpoint
```powershell
curl http://localhost:8000/
```

**Ожидаемый ответ:**
```
WhatsApp Bot is running! 🤖
```

### Проверка ngrok туннеля
Откройте в браузере: http://localhost:4040

Должен открыться Web Interface ngrok с активным туннелем.

---

## 📊 Структура данных

### data/attendance.json

После первой записи смены файл будет выглядеть так:

```json
{
  "79991234567": [
    {
      "date": "2025-11-09",
      "shift": "Дневная (08:00–20:00)"
    },
    {
      "date": "2025-11-08",
      "shift": "Выходной"
    }
  ]
}
```

**Формат:**
- Ключ: номер телефона (без `+`)
- Значение: массив записей с полями `date` (YYYY-MM-DD) и `shift` (название смены)

---

## ⚠️ Частые проблемы

### Проблема: "VERIFY_TOKEN не найден"
**Решение:**
- Убедитесь что файл `.env` существует в корне проекта
- Проверьте что в `.env` есть строка `VERIFY_TOKEN=значение`
- Убедитесь что нет пробелов вокруг `=`

### Проблема: "D360_API_KEY не найден"
**Решение:**
- Проверьте что в `.env` есть строка `D360_API_KEY=ваш_ключ`
- Скопируйте ключ из панели 360dialog (hub.360dialog.com → API Keys)

### Проблема: 403 при верификации вебхука
**Решение:**
- VERIFY_TOKEN в `.env` должен **точно** совпадать с токеном в 360dialog
- Проверьте что нет лишних пробелов или символов новой строки

### Проблема: Бот не отвечает в WhatsApp
**Решение:**
1. Проверьте логи Flask — должны быть POST запросы от 360dialog
2. Проверьте что ngrok активен (http://localhost:4040)
3. Проверьте что URL в 360dialog правильный и содержит `/webhook`
4. Проверьте что события "Incoming messages" включены в 360dialog
5. Убедитесь что Flask возвращает 200 OK на POST /webhook

### Проблема: ngrok URL изменился
**Решение:**
- Бесплатный ngrok меняет URL при каждом перезапуске
- Нужно обновить URL в панели 360dialog
- Для постоянного URL используйте платный ngrok или VPS

---

## 🎯 Следующие шаги

После успешного запуска можете:

1. **Добавить новые функции:**
   - Заполнение за период дат
   - Экспорт в Google Sheets
   - Отчеты для администраторов

2. **Улучшить учет:**
   - Добавить причину выходного
   - Указать количество часов
   - Добавить комментарии к записям

3. **Деплой в production:**
   - Арендовать VPS
   - Настроить HTTPS через Let's Encrypt
   - Запустить через gunicorn + systemd
   - Настроить мониторинг и алерты

4. **Интеграции:**
   - Google Sheets для отчетов
   - Telegram для уведомлений админов
   - База данных (PostgreSQL) вместо JSON

---

## 📞 Контакты и поддержка

- **Документация:** `README.md`
- **Быстрый старт:** `QUICKSTART_RUS.md`
- **Тестирование:** `scripts/mock_payloads.py`
- **360dialog:** https://hub.360dialog.com
- **ngrok:** https://ngrok.com

---

**Версия:** 1.0.0  
**Дата:** 2025-11-09  
**Статус:** ✅ Готов к запуску

