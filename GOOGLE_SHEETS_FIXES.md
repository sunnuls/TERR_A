# 🔧 Исправления Google Sheets

## ✅ Что исправлено:

### 1. Возврат в меню
- После экспорта бот теперь автоматически возвращает в главное меню

### 2. Дубликаты таблиц
- Исправлена логика создания таблиц
- Теперь проверяется существование перед созданием
- Больше не будет создаваться 3 копии

### 3. Перемещение в папку
- Исправлена логика перемещения файлов
- Теперь таблицы правильно перемещаются в указанную папку
- Удаляются из корня Drive

## 📝 Что нужно сделать:

### 1. Удалите дубликаты таблиц вручную

В Google Drive у вас 3 таблицы "WorkLog - November 2025":
1. Откройте [Google Drive](https://drive.google.com/)
2. Найдите 3 таблицы с одинаковым названием
3. Удалите 2 из них (оставьте одну)
4. Очистите корзину

### 2. Создайте папку для отчетов (опционально)

Если хотите, чтобы таблицы создавались в отдельной папке:

1. Откройте [Google Drive](https://drive.google.com/)
2. Создайте папку (например, "WorkLog Reports")
3. Откройте папку
4. Скопируйте ID из URL:
   ```
   https://drive.google.com/drive/folders/1AbCdEfGhIjKlMnOpQrStUvWxYz
                                          ^^^^^^^^^^^^^^^^^^^^^^^^
                                          Это DRIVE_FOLDER_ID
   ```
5. Добавьте в `.env`:
   ```env
   DRIVE_FOLDER_ID=1AbCdEfGhIjKlMnOpQrStUvWxYz
   ```

### 3. Очистите базу данных от старых записей экспорта

```bash
# Остановите бота (Ctrl+C)

# Откройте базу данных и очистите таблицы экспорта
sqlite3 reports_whatsapp.db
```

В SQLite консоли выполните:
```sql
DELETE FROM google_exports;
DELETE FROM monthly_sheets;
.quit
```

### 4. Перезапустите бота

```bash
python bot.py
```

### 5. Попробуйте экспорт снова

1. Откройте бота в WhatsApp
2. `Ещё...` → `Админ` → `Экспорт`
3. Должно экспортироваться все ваши записи
4. Проверьте Google Sheets - данные должны появиться

---

## 🐛 Почему таблицы были пустые?

Проблема была в том, что при нажатии кнопки "Экспорт":
- Создавалась новая таблица
- Но данные не экспортировались, потому что бот думал, что они уже экспортированы

После очистки таблиц `google_exports` и `monthly_sheets` бот заново экспортирует все записи.

---

## ✅ После исправлений:

- ✅ Бот возвращает в меню после экспорта
- ✅ Создается только одна таблица на месяц
- ✅ Данные экспортируются правильно
- ✅ Таблицы перемещаются в указанную папку (если настроено)
- ✅ При создании нового отчета он автоматически экспортируется
- ✅ При изменении/удалении отчета изменения синхронизируются

---

**Перезапустите бота и попробуйте снова!** 🚀
