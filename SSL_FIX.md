# 🔧 Исправление SSL ошибки и множественных экспортов

## Проблемы:

1. ❌ SSL ошибка: `[SSL: WRONG_VERSION_NUMBER]`
2. ❌ Бот отправляет 5 сообщений "Экспортирую..."
3. ❌ Множественные попытки создания таблицы

## ✅ Что исправлено в коде:

1. Добавлена блокировка (`threading.Lock`) для предотвращения одновременных экспортов
2. Теперь если экспорт уже идет, новые попытки игнорируются

## 🔧 Исправление SSL ошибки:

### Вариант 1: Обновите библиотеки (рекомендуется)

```bash
# Остановите бота (Ctrl+C)

# Обновите SSL библиотеки
pip install --upgrade certifi urllib3
pip install --upgrade google-auth google-auth-oauthlib google-auth-httplib2 google-api-python-client

# Перезапустите бота
python bot.py
```

### Вариант 2: Проверьте антивирус/файрвол

Некоторые антивирусы (Kaspersky, Avast) перехватывают SSL соединения.

**Временно отключите антивирус** и попробуйте экспорт снова.

### Вариант 3: Проверьте прокси/VPN

Если используете VPN или прокси - отключите их.

### Вариант 4: Используйте другую сеть

Попробуйте подключиться через мобильный интернет (раздача с телефона).

## 🔧 Исправление множественных сообщений:

Код уже исправлен - теперь используется блокировка.

**Перезапустите бота:**
```bash
python bot.py
```

## 📝 Если SSL ошибка не исчезает:

### Проверьте системное время

Неправильное время может вызывать SSL ошибки:
```bash
# Windows
w32tm /resync

# Или через настройки:
# Параметры → Время и язык → Синхронизировать часы
```

### Проверьте доступ к Google API

Откройте браузер и проверьте доступ к:
- https://sheets.googleapis.com/
- https://www.googleapis.com/

Если не открывается - проблема в сети/файрволе.

## 🎯 Быстрое решение:

1. **Обновите библиотеки:**
   ```bash
   pip install --upgrade certifi urllib3 google-auth google-auth-oauthlib google-api-python-client
   ```

2. **Перезапустите компьютер** (иногда помогает)

3. **Попробуйте другую сеть** (мобильный интернет)

4. **Временно отключите антивирус**

5. **Перезапустите бота:**
   ```bash
   python bot.py
   ```

---

## ✅ После исправления:

- Бот будет отправлять только одно сообщение "Экспортирую..."
- Таблица будет создаваться только один раз
- SSL ошибка должна исчезнуть

**Попробуйте сначала обновить библиотеки!** 🚀
