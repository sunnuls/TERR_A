📝 **Инструкция: Настройка Google Sheets**

## Проблема решена! ✅

Файл `oauth_client.json` был переименован правильно.

## Следующие шаги:

### 1. Добавьте настройки в .env файл

Откройте `.env` и добавьте (или проверьте) эти строки:

```env
# Google Sheets настройки
OAUTH_CLIENT_JSON=oauth_client.json
TOKEN_JSON_PATH=token.json
DRIVE_FOLDER_ID=
EXPORT_PREFIX=WorkLog
AUTO_EXPORT_ENABLED=false
AUTO_EXPORT_CRON=0 9 * * 1
```

**Примечание:** `DRIVE_FOLDER_ID` можно оставить пустым, таблицы будут создаваться в корне вашего Google Drive.

### 2. Перезапустите бота

Остановите бота (Ctrl+C) и запустите заново:

```bash
python bot.py
```

### 3. Пройдите OAuth авторизацию

При первом запуске:
1. Откроется браузер с запросом авторизации Google
2. Войдите в свой Google аккаунт
3. Разрешите доступ к Google Sheets и Google Drive
4. Закройте браузер
5. Токен сохранится в `token.json`

### 4. Проверьте работу

После авторизации в логах должно появиться:
```
✅ Google Sheets готов к работе
```

Теперь попробуйте снова нажать кнопку "Экспорт" в боте.

---

## Если возникнут проблемы:

### Ошибка: "redirect_uri_mismatch"

В Google Cloud Console:
1. Перейдите в `APIs & Services` → `Credentials`
2. Найдите ваш OAuth 2.0 Client ID
3. Добавьте в "Authorized redirect URIs":
   - `http://localhost`
   - `http://localhost:8080`

### Ошибка: "Access blocked"

Убедитесь, что в Google Cloud Console включены:
- Google Sheets API
- Google Drive API

---

**После перезапуска бота Google Sheets будет работать!** 🎉
