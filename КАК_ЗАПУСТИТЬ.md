# 🚀 ИНСТРУКЦИЯ: Как запустить бота

## 📋 Что нужно сделать:

### 1. Настройте файл .env

Откройте файл `.env` в корне проекта и заполните **ОБЯЗАТЕЛЬНЫЕ** параметры:

```env
# ОБЯЗАТЕЛЬНО - получите в 360dialog или Meta Developers
WHATSAPP_TOKEN=ваш_токен_здесь
WHATSAPP_PHONE_ID=ваш_phone_id_здесь

# ОБЯЗАТЕЛЬНО - придумайте свой секретный токен
VERIFY_TOKEN=terra_verify123

# Сервер (можно оставить по умолчанию)
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# Админы (ваш номер телефона)
ADMIN_IDS=79991234567

# Таймзона
TZ=Europe/Moscow
```

### 2. Где взять токены:

**WHATSAPP_TOKEN и WHATSAPP_PHONE_ID:**
- Зайдите на https://hub.360dialog.com/
- Войдите в аккаунт
- Выберите ваш WhatsApp номер
- Скопируйте **API Key** → это `WHATSAPP_TOKEN`
- Скопируйте **Phone Number ID** → это `WHATSAPP_PHONE_ID`

**VERIFY_TOKEN:**
- Придумайте сами (любые буквы и цифры)
- Например: `terra_verify123`

### 3. Установите зависимости:

```powershell
pip install -r requirements_whatsapp.txt
```

### 4. Запустите бота:

```powershell
python bot_polya_whatsapp.py
```

### 5. Настройте webhook (для получения сообщений):

**Вариант А: Через ngrok (для локального тестирования):**

1. Запустите ngrok в отдельном окне:
```powershell
ngrok http 8000
```

2. Скопируйте HTTPS URL (например: `https://abc123.ngrok-free.app`)

3. Настройте webhook в 360dialog:
   - Зайдите на https://hub.360dialog.com/
   - Перейдите в Webhooks/Configuration
   - URL: `https://abc123.ngrok-free.app/webhook`
   - Verify Token: `terra_verify123` (тот же, что в .env)

**Вариант Б: Если у вас есть сервер с HTTPS:**
- Укажите ваш домен в настройках webhook

### 6. Проверьте работу:

1. Отправьте боту `start` в WhatsApp
2. Бот должен ответить и попросить ввести имя
3. Введите Фамилию Имя
4. Используйте меню для работы

## ⚠️ Важно:

- ✅ Файл `.env` должен быть в корне проекта
- ✅ `WHATSAPP_TOKEN` и `WHATSAPP_PHONE_ID` - ОБЯЗАТЕЛЬНЫ
- ✅ Сервер должен быть доступен по HTTPS для webhook
- ✅ Не закрывайте окно с ботом - пока оно открыто, бот работает

## 🔍 Проверка:

После запуска вы должны увидеть:
```
🤖 WhatsApp бот запущен!
📡 Слушаю на 0.0.0.0:8000
```

Если видите ошибки:
- ❌ "WHATSAPP_TOKEN не найден" → заполните .env
- ❌ "WHATSAPP_PHONE_ID не найден" → заполните .env
- ❌ "ModuleNotFoundError" → установите зависимости: `pip install -r requirements_whatsapp.txt`

